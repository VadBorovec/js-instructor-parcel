// const Joi = require("joi");
// const shortid = require("shortid");

// const passwordSchema = Joi.string().min(3).max(10).alphanum();

// console.log(passwordSchema.validate("124"));

// console.log(shortid.generate());

export function add(a, b) {
  return a + b;
}

console.log(add(2, 7));
